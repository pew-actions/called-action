{
  "name": "called-action",
  "version": "1.0.0",
  "description": "GitHub action to gather information on the called action",
  "main": "lib/main.js",
  "scripts": {
    "build": "eslint src/ && tsc && ncc build",
    "watch-build": "nodemon --watch src --ext ts --exec npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@types/node": "^22.8.1",
    "@vercel/ncc": "^0.36.1",
    "eslint": "^9.13.0",
    "nodemon": "^3.1.7",
    "typescript": "^4.8.4",
    "typescript-eslint": "^8.11.0"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "engines": {
    "node": ">= 20"
  }
}
